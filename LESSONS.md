# LESSONS — 実際に作って分かったこと

**資料に書いた内容が、現実に動かしたら違っていた記録。**

`0-core/` や `1-web-site/` は「こうすべき」を書く場所。
ここは **「こう書いたら、こうハマった」** を書く場所。追記していく。

書き方：`症状 → 原因 → 対処 → 教訓`。日付を入れる。
**推測で書かない。実際に踏んだものだけ。**

---

## 2026-07-31

### L-001. Tailwind CDN の preflight は、`<link>` した CSS に後勝ちする

**症状**
`japanese-text.css` に `body { line-height: 1.7 }` と書いたのに、
実測すると 24px（= 1.5）。日本語の行間指定がまったく効いていなかった。

**原因**
Tailwind CDN（`<script src="https://cdn.tailwindcss.com">`）は
preflight を**実行時に `<head>` の末尾へ `<style>` として注入する**。
`<link rel="stylesheet">` は HTML パース時点で head に入るので、**常に手前**。
詳細度が同じ（要素セレクタ `body` = 0,0,1）だと、**後から来た preflight が勝つ**。

Tailwind preflight は `html { line-height: 1.5 }` と `body { line-height: inherit }` を持つ。

**対処**
継承で効かせたいものは `:root`（擬似クラス = 0,1,0）に指定する。

```css
:root { line-height: 1.7; font-family: var(--font-sans); }
```

**教訓**
CDN 版のライブラリは「読み込み順 = ファイルの並び順」ではない。
**実行時に注入されるものは常に最後に来る**と考える。
ビルドして使えるなら `@layer base` に入れるのが正しい解で、詳細度をいじる必要はない。

---

### L-002. 詳細度を上げすぎると、今度はユーティリティを潰す

**症状**
L-001 の対処として `:root h1 { font-size: clamp(...) }` と書いたら、
HTML 側の `class="text-4xl md:text-6xl"` が効かなくなった。

**原因**
`:root h1` は (0,1,1)。Tailwind のユーティリティ `.text-6xl` は (0,1,0)。
**base 側が勝ってしまう。** base は「ユーティリティに負ける」のが正しい姿。

**対処**
詳細度を上げる範囲を絞る。

| 対象 | 扱い |
|---|---|
| 継承されるもの（`line-height` / `font-family`） | `:root` に上げてよい。子要素のユーティリティが継承を上書きするので害がない |
| 要素そのものに当たるもの（見出しの `font-size`、`body` の背景色） | **上げない。** ユーティリティを潰す |

`:root body { background: … }` も同じ理由で不可（`.bg-*` が効かなくなる）。

**教訓**
**「base は弱く、ユーティリティは強く」の順序を壊さない。**
preflight に勝ちたい気持ちで一律に詳細度を上げると、別の場所が壊れる。
継承プロパティかどうかで判断する。

---

### L-003. ブランド色 `#A9786F` に白文字は、AA を満たさない

**症状**
塗りつぶしボタンの背景に `#A9786F`、文字を白にしたところ、
コントラスト比が **約 3.75:1**。

**原因**
WCAG 1.4.3 の要求は、通常テキスト **4.5:1**、大きい文字（18pt以上/太字14pt以上）3:1。
ボタンのラベルは 16px 程度＝**通常テキスト扱い**なので 4.5:1 が必要。
`#A9786F` は大きい見出しには使えるが、ボタンラベルには足りない。

**対処**
白文字用に暗い変種 `#96685F`（約 **4.7:1**）を用意し、
`--action-primary`（塗りつぶしボタン）に割り当てた。
`#A9786F` は `--action-accent`（大きい見出し・装飾）に回した。

**教訓**
**ブランドカラーは「そのまま全用途に使える」とは限らない。**
トークン設計の段階で「白文字を載せられる濃さ」を1段用意しておく。
※ 数値は sRGB 相対輝度から算出（WCAG 2.x の定義）。DevTools のカラーピッカーでも確認できる。

---

### L-004. ラップ型 `<label>` の中の入力は、ラベル名を取り出せない

**症状**
チェックボックスのエラーが「**この項目**に同意してください」と表示された。

**原因**
バリデーション側は `label[for="id"]` を探してラベル名を得る実装だった。
同意チェックボックスは `<label>` で**包む**形（`for` なし）だったため見つからず、
フォールバックの「この項目」になった。
かといって包んでいる `<label>` の全文を使うと
「プライバシーポリシーに同意するに同意してください」になる。

**対処**
`data-error-label="プライバシーポリシー"` を入力側に付け、それを最優先で使う。
フォールバック順は `data-error-label` → `label[for]` → `closest('label')` → 既定文言。

**教訓**
**ラベルの取得方法は1つではない。** アクセシブルな名前の決まり方
（`aria-label` / `aria-labelledby` / `label[for]` / ラップ型）を全部想定する。
エラー文にラベルを流用する設計は、**短い名前を別に渡せる逃げ道**を用意しておく。

---

### L-005. 静的サーバーで `/` を書き換えると、相対パスが全滅する

**症状**
`http://localhost:4322/` を開くと HTML は表示されるが、
CSS も JS も一切効かない。コンソールにエラーは出ない。

**原因**
サーバー側で `/` → `/snippets/index.html` と**内部書き換え**していた。
ブラウザから見た URL は `/` のままなので、
ページ内の相対パス `css/tokens.css` は `/css/tokens.css` に解決され、**全部 404**。

**対処**
書き換えではなく **302 リダイレクト**にして、ブラウザの URL 自体を `/snippets/` にする。

**教訓**
**「表示された = 正しく読み込めた」ではない。**
CSS/JS が 404 でも HTML は表示されるし、コンソールにエラーが出ないこともある。
`getComputedStyle` で実測するか、Network タブを見るまで信用しない。

---

### L-006（これが一番大きい）. 書いた資料は、動かすまで信用しない

**症状**
`0-core/` と `1-web-site/` を書き終えた時点では、内容に自信があった。
`snippets/` として実際に動かしたら、**上の L-001〜L-005 が出た。**

**原因**
資料は「正しそうなこと」を書けてしまう。
特に**複数のものを組み合わせたときに初めて壊れる**類（CSS の詳細度、読み込み順、
ライブラリ同士の衝突）は、文章を読み返しても絶対に見つからない。

**対処**
資料に実装コードを書いたら、**必ず動く形にして 1 回は開く。**
確認は目視だけでなく、値を実測する。

```js
getComputedStyle(document.body).lineHeight   // "27.2px" ← 1.7 が効いている
document.documentElement.scrollWidth         // 320 ← 横スクロールなし
```

**教訓**
**この資料の信頼性は、`snippets/` が動いていることで担保されている。**
資料を追記したら、対応するスニペットも更新して開き直す。
片方だけ育てると、資料が「動かないことが書いてある本」になる。

---

## 2026-08-01

### L-007. 自分が作った見本帳が、AIデフォルトの見た目そのものだった

**症状**
`snippets/index.html` を「整っている」と思って出した。
後日 Anthropic の `frontend-design` skill を読んだところ、
**AI 生成デザインが収束する3つの型**が挙げられており、その1番目が

> 温かいクリーム色の背景（`#F4F1EA` 付近）＋ 高コントラストのセリフ見出し ＋ テラコッタのアクセント

これは自分が使った ivory `#F7F3EE` ＋ Cormorant Garamond ＋ rose `#A9786F` と**完全に一致**していた。

**原因**
資料に「安っぽさの回避」（種類を数える／余白／階層）は書いたが、
**「既定値に流れることの回避」を書いていなかった。**
`0-core` と `1-web-site` の指示に忠実に従うほど、無難で似た見た目に収束する。
**ルールを守ることと、選択をすることは別。**

さらに悪いことに、**この資料自体が「次に作るときの参照元」**なので、
放置すると同じ見た目を再生産し続ける装置になっていた。

**対処**
- `visual-design.md` に §0「AIっぽさを避ける」を新設。
  3つのデフォルトを列挙し、抜け出し方（題材の世界から取る／シグネチャーを1つ／
  構造に意味を持たせる／書体を選び直す）を書いた
- `process.md` ⑤に**2段階**を導入。計画（Color / Type / Layout / **Signature**）を先に文章で書き、
  **作る前に「誰でも同じ結論に至りそうな部分」を自分で叩く**
- `snippets/README.md` に「あの配色は Knacit のブランド例であって推奨デフォルトではない」と明記

**教訓**
**「ルールに違反していない」と「良い」は違う。**
チェックリストは減点を防ぐが、加点は生まない。
参照資料は放っておくと**平均への回帰装置**になる。
だから資料には「守るべき線」だけでなく、
**「毎回ここは選び直せ」という欄**が要る。

自己点検の問い：
> **同じ指示を別の誰かに出したら、同じものが出てくるか。**
> 出てくるなら、それは選択ではなく既定値。

---

### L-008. ダークモードのコントラストを一度も測っていなかった

**症状**
テーマ比較ページに**コントラスト自動計測**を仕込んだところ、
`--border-strong`（部品の境界線）が背景に対して **1.81:1** しかないことが判明した。
基準は 3:1（WCAG 1.4.11）。

**原因**
2つ重なっていた。

1. **ダークモードの値を勘で決めていた。** ライトモードの値は L-003 で実測したが、
   `@media (prefers-color-scheme: dark)` 側は「暗いから薄い罫線でいいだろう」で決めていた。
   実測すると `#4A423C` on `#1A1715` = 1.81:1 で全く足りない
2. **`--border-strong` という名前なのに 3:1 を満たしていなかった。**
   ライトモードの `#C4B8A9` も 1.8:1。装飾の区切り線としては十分だが、
   **入力欄やチェックボックスの枠に使うと「どこが枠か分からない」**

**対処**
- 罫線トークンの役割を分けて明記した
  - `--border-default` … カードの囲み・区切り線（**装飾**。3:1 不要）
  - `--border-strong` … **入力欄・チェックボックスの境界**（3:1 必須）
- 各テーマに 3:1 を満たす値を用意（light `#8E8375` 3.7:1 / dark `#7A6F66` 3.6:1）
- **ページ自身がコントラストを計算して合否を表示する**ようにした（`snippets/themes.html`）

**教訓**
**ダークモードは「別のデザイン」であって、明るい色の反転ではない。**
ライトで測ったなら、ダークでも測る。両方測るまで終わっていない。

そして**検査を人間の手順書に書くと、いつか飛ばす。**
`launch-checklist.md` に「コントラストを測る」と書いてあったのに、自分で飛ばしていた。
**ページ自身に検査を埋め込むと、開くたびに自動で走る。** これが一番確実だった。

---

### L-009. 白背景でだけ測っても足りない — 背景ごとに測る

**症状**
`#96685F` は白背景で 4.70:1 を確認して採用した（L-003）。
その後 `audit.js` を作って実際の描画から測ったところ、
**帯背景 `#F7F3EE` の上では 4.28:1** で基準未満だった。
セクションラベルなど、帯の上に置く小さい文字が全滅していた。

同じ検査で、もっと分かりやすい違反も出た。

| 箇所 | 実測 | 原因 |
|---|---|---|
| CTAバンドの補足文 `text-white/80` | 4.05:1 | **不透明度を下げて薄くしていた** |
| CTAバンドの注記 `text-white/70` | 3.51:1 | 同上 |
| industrial テーマのラベル（アンバー13px） | 3.46:1 | UI部品用の色を小さい文字に使った |

**しかも `text-white/80` は自分のドキュメント（`layout-patterns.md`）が教えていた書き方だった。**
資料が間違ったパターンを配っていた。

**原因**
2つある。

1. **測る背景を1つに決めていた。** 実際のページには白・帯・濃色CTA・カードと複数の背景がある。
   同じ文字色でも背景が変われば比が変わる
2. **トークンの役割分けが足りなかった。** `--action-accent` は
   「UI部品・大きい文字（3:1）」用なのに、小さい文字にも使えると思っていた

**対処**
- `--c-primary-600` を `#96685F` → **`#8E6058`** に変更
  （白 5.30:1 / 帯 4.79:1 / ボタン背景としても 5.30:1 — すべて合格）
- **`--text-accent` を新設**（小さい文字用・4.5:1 保証）。
  `--action-accent`（UI部品・大きい文字用・3:1）と役割を分離
- `text-white/70` `/80` を **`/90`**（4.65:1）に。資料側の記述も同時に修正
- 罫線も同様に `--border-default`（装飾）と `--border-strong`（部品の境界・3:1）に分離

**教訓**
**色は単体では合否が決まらない。「その色を、どの背景に、何pxで置くか」まで含めて初めて決まる。**
だからトークンは色だけでなく**用途で分ける**：

```
--action-primary   ボタン背景（白文字を載せて 4.5:1）
--action-accent    UI部品・アイコン・大きい文字（3:1）
--text-accent      小さい文字（4.5:1）
--border-default   装飾の区切り（基準なし）
--border-strong    部品の境界（3:1）
```

**不透明度で文字を薄くしない。** `/70` `/80` は手軽だが、ほぼ必ず基準を割る。
弱く見せたいなら**サイズか配置**で差をつける。

---

### L-010. 検査ツール自身にバグがあった

**症状**
`audit.js` を作って自分のページに当てたら、実バグと**同時に誤検出も3件**出た。

| 誤検出 | 原因 |
|---|---|
| 「`transition: all` を使っている」が全要素で出る | `transition-property` の **CSS初期値が `all`**。duration が 0 かどうかを見ていなかった |
| 大きな数値表示（30px）が「行間 1.20 で不足」 | 見出し・数値は行間を詰めるのが正しい。サイズで除外していなかった |
| 1行で収まる短い注記が「行間不足」 | **折り返さないテキストに行間は関係ない** |
| 文中のリンクが「タップ領域不足」 | WCAG 2.5.8 のインライン例外を、祖先タグ（p/li）だけで判定していた。`<label>` 内を拾えていない |
| チェックボックスが「16px未満でiOSズームする」 | **iOSの自動ズームはテキスト入力系だけ**。チェックボックスは対象外 |

**対処**
- `transitionDuration > 0` を条件に追加
- 行間チェックを「24px未満 **かつ** 2行以上折り返している」ものに限定
- インライン例外を「前後に地の文があるか」で判定するよう変更
- 16px チェックを text/email/tel/url/password/textarea 等に限定

**教訓**
**検査ツールを信じすぎない。誤検出を放置すると、本物の警告まで無視するようになる。**
新しいルールを足したら、**必ず「出るはずのないページ」でも走らせて**、
0件になることを確認する。

自動検査で拾えるのは全体の一部でもある。
キーボードでの通し操作、読み上げ、文章の質は人間が見るしかない。

---

## 追記のしかた

1. 実際に踏んだら、その場でここに書く（記憶が新しいうちに）
2. `症状 → 原因 → 対処 → 教訓` の順を守る
3. 該当する資料ファイル側にも**短い注記**を入れて、ここへのリンクを張る
4. **推測やベストプラクティスの引き写しは書かない。** それは `0-core/` `1-web-site/` の役目
