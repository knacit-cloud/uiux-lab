# AGENTS.md — AI へのルール

このリポジトリを参照して UI を作る／レビューするときに守ること。
`CLAUDE.md` としても機能する（Claude Code は AGENTS.md を読む）。

---

## 0. 最初にやること

作業を始める前に、**どの対象か**を確定させる。

| 作るもの | 読むフォルダ |
|---|---|
| LP・コーポレートサイト | `0-core/` + `1-web-site/` |
| ダッシュボード・管理画面 | `0-core/` + `2-web-app/` |
| ネイティブアプリ | `0-core/` + `3-mobile/` |

**対象外のフォルダのパターンを持ち込まない。**
管理画面のデータテーブル設計を LP に持ち込む、LP のヒーロー演出を管理画面に持ち込む、
といった混在がこの資料が防ごうとしている一番の失敗。

対象が判断できないときは、勝手に決めずにユーザーに聞く。

---

## 1. 絶対に破らない線（違反したら作り直し）

これは「できれば」ではなく要件。

- **本文フォントサイズは 16px 以上。** 14px は footer など限定用途のみ。14px 未満は使わない
- **本文の行間は font-size の 1.5 倍以上**（日本語は特に。`line-height: 1.6` 程度を既定に）
- **テキストのコントラスト比 4.5:1 以上**（大きい文字は 3:1 以上）
- **UI部品・アイコンのコントラスト比 3:1 以上**
- **タップ／クリック領域は 24×24 CSS px 以上**（モバイル主体なら 44×44 を目標）
- **フォーカスが見えること。** `outline: none` を書くなら `:focus-visible` の代替を必ず用意する
- **`prefers-reduced-motion` を尊重する**
- **アクションは `<button>`、遷移は `<a>`。** `<div onClick>` は使わない
- **アイコンのみのボタンには `aria-label`**
- **`<img>` に `width` / `height` を明示**（レイアウトシフト防止）
- **`user-scalable=no` / `maximum-scale=1` は禁止**

根拠と詳細：`0-core/accessibility.md`、`0-core/typography.md`

---

## 2. 迷ったときの既定値

指示がない限りこれを使う。

- 余白は **8px の倍数**（4px は微調整のみ）
- アニメーションは **`transform` と `opacity` のみ**。`transition: all` は書かない
- 遷移時間は **150–250ms**。それ以上は遅く感じる
- 角丸・影は**用途ごとに一貫**させる。1ページに3種類以上の角丸を混ぜない
- 1行の文字数は **日本語 35–45字 / 欧文 45–75字** に収まる幅にする
- 色は**トークン（CSS変数 or Tailwind config）経由**で使う。生の hex を JSX/HTML に直書きしない

---

## 3. 出力するとき

- **既存プロジェクトの流儀を優先する。** この資料はデフォルト値であって、
  プロジェクト側に確立したトークン・命名・書き方があるならそちらに従う
- コードを書く前に、この資料のどのルールを適用したか一言添える
- ルールを意図的に外すときは、外した理由を明示する。黙って外さない

---

## 3.5. 検証に使う素材の扱い

作った資料やチェックリストが実際に使えるかを試すとき、
**既存の実サイトを題材として読むのはよい。編集は絶対にしない。**

| サイト | 読む | 書く |
|---|---|---|
| `~/Desktop/Bloomer site`（Knacit 本番 https://knacit.com） | ✅ 検証の題材として可 | ❌ **一切不可**（設定ファイル含む） |

「試験として利用するのであって、編集するためではない」。
指摘や改善案は**このリポジトリ側にメモとして書く**。実サイトには手を出さない。
実サイトを直すのは、ユーザーがそのサイトの作業を明示的に依頼したときだけ。

**デモ・サンプルを作るときは、何のために作るのかを先に伝える。**
説明なしに完成品を出すと、意図を誤解される（`snippets/index.html` で実際に起きた）。

---

## 4. この資料に追記するとき

- **出典なしの断定を書かない。** 一次資料のURLを添える
- 検証記号を守る：`⚠️`（AI照合済・人間未確認）／`✅`（人間確認済）／`🔍`（リンクのみ）。
  **AI は自分で `✅` を付けてはいけない**
- 新しい主張を足したら `references/sources.md` にも出典を登録する
- 対象フォルダの `SCOPE.md` に反する内容を、そのフォルダに書かない
- **実装コードを書いたら、`snippets/` で動く形にして 1 回は開く。**
  値を実測する（`getComputedStyle` / `scrollWidth`）。目視だけで済ませない
- **実際に踏んだ不具合は `LESSONS.md` に追記する。**
  `症状 → 原因 → 対処 → 教訓` の順。推測やベストプラクティスの引き写しは書かない
  （それは `0-core/` `1-web-site/` の役目）

---

## 5. レビューを頼まれたとき

`0-core/implementation-checklist.md` を上から当てる。
指摘は `file:line` 形式で、**何が問題か・どう直すか**をセットで書く。
「気になる」レベルの主観は出さない。上記「絶対に破らない線」に触れるものを最優先で報告する。
